<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.bonitasoft.connectors</groupId>
	<artifactId>bonita-connector-zendesk</artifactId>
	<version>1.0.0-beta.1</version>

	<name>Bonita Zendesk Connector</name>
	<description>Zendesk Support Connectors for Bonita</description>
	<url>https://www.bonitasoft.com</url>

	<developers>
		<developer>
			<id>bonitasoft</id>
			<name>Bonitasoft</name>
			<email>rd.user@bonitasoft.com</email>
			<organization>Bonitasoft</organization>
			<organizationUrl>https://www.bonitasoft.com</organizationUrl>
		</developer>
	</developers>

	<licenses>
		<license>
			<name>GPL-v2.0</name>
			<url>http://www.gnu.org/licenses/gpl-2.0.txt</url>
		</license>
	</licenses>

	<properties>
		<!-- Maven -->
		<java.version>17</java.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<maven.build.timestamp.format>yyyy.MM.dd-hh.mm</maven.build.timestamp.format>

		<!-- Create Ticket -->
		<zendesk-create-ticket.def.id>com.bonitasoft.connectors.zendesk.CreateTicketDefinition</zendesk-create-ticket.def.id>
		<zendesk-create-ticket.def.version>1.0.0</zendesk-create-ticket.def.version>
		<zendesk-create-ticket.impl.id>${zendesk-create-ticket.def.id}-impl</zendesk-create-ticket.impl.id>
		<zendesk-create-ticket.impl.version>${project.version}</zendesk-create-ticket.impl.version>
		<zendesk-create-ticket.main-class>com.bonitasoft.connectors.zendesk.CreateTicketConnector</zendesk-create-ticket.main-class>

		<!-- Update Ticket -->
		<zendesk-update-ticket.def.id>com.bonitasoft.connectors.zendesk.UpdateTicketDefinition</zendesk-update-ticket.def.id>
		<zendesk-update-ticket.def.version>1.0.0</zendesk-update-ticket.def.version>
		<zendesk-update-ticket.impl.id>${zendesk-update-ticket.def.id}-impl</zendesk-update-ticket.impl.id>
		<zendesk-update-ticket.impl.version>${project.version}</zendesk-update-ticket.impl.version>
		<zendesk-update-ticket.main-class>com.bonitasoft.connectors.zendesk.UpdateTicketConnector</zendesk-update-ticket.main-class>

		<!-- Get Ticket -->
		<zendesk-get-ticket.def.id>com.bonitasoft.connectors.zendesk.GetTicketDefinition</zendesk-get-ticket.def.id>
		<zendesk-get-ticket.def.version>1.0.0</zendesk-get-ticket.def.version>
		<zendesk-get-ticket.impl.id>${zendesk-get-ticket.def.id}-impl</zendesk-get-ticket.impl.id>
		<zendesk-get-ticket.impl.version>${project.version}</zendesk-get-ticket.impl.version>
		<zendesk-get-ticket.main-class>com.bonitasoft.connectors.zendesk.GetTicketConnector</zendesk-get-ticket.main-class>

		<!-- Add Comment -->
		<zendesk-add-comment.def.id>com.bonitasoft.connectors.zendesk.AddCommentDefinition</zendesk-add-comment.def.id>
		<zendesk-add-comment.def.version>1.0.0</zendesk-add-comment.def.version>
		<zendesk-add-comment.impl.id>${zendesk-add-comment.def.id}-impl</zendesk-add-comment.impl.id>
		<zendesk-add-comment.impl.version>${project.version}</zendesk-add-comment.impl.version>
		<zendesk-add-comment.main-class>com.bonitasoft.connectors.zendesk.AddCommentConnector</zendesk-add-comment.main-class>

		<!-- Search Tickets -->
		<zendesk-search-tickets.def.id>com.bonitasoft.connectors.zendesk.SearchTicketsDefinition</zendesk-search-tickets.def.id>
		<zendesk-search-tickets.def.version>1.0.0</zendesk-search-tickets.def.version>
		<zendesk-search-tickets.impl.id>${zendesk-search-tickets.def.id}-impl</zendesk-search-tickets.impl.id>
		<zendesk-search-tickets.impl.version>${project.version}</zendesk-search-tickets.impl.version>
		<zendesk-search-tickets.main-class>com.bonitasoft.connectors.zendesk.SearchTicketsConnector</zendesk-search-tickets.main-class>

		<!-- List Ticket Comments -->
		<zendesk-list-ticket-comments.def.id>com.bonitasoft.connectors.zendesk.ListTicketCommentsDefinition</zendesk-list-ticket-comments.def.id>
		<zendesk-list-ticket-comments.def.version>1.0.0</zendesk-list-ticket-comments.def.version>
		<zendesk-list-ticket-comments.impl.id>${zendesk-list-ticket-comments.def.id}-impl</zendesk-list-ticket-comments.impl.id>
		<zendesk-list-ticket-comments.impl.version>${project.version}</zendesk-list-ticket-comments.impl.version>
		<zendesk-list-ticket-comments.main-class>com.bonitasoft.connectors.zendesk.ListTicketCommentsConnector</zendesk-list-ticket-comments.main-class>

		<!-- Bonita -->
		<bonita-common.version>10.2.0</bonita-common.version>

		<!-- Connector dependencies -->
		<lombok.version>1.18.30</lombok.version>
		<jackson.version>2.17.0</jackson.version>
		<slf4j.version>2.0.12</slf4j.version>

		<!-- Test -->
		<junit-jupiter.version>5.10.2</junit-jupiter.version>
		<mockito.version>5.11.0</mockito.version>
		<assertj.version>3.27.7</assertj.version>
		<jqwik.version>1.8.4</jqwik.version>
		<wiremock.version>3.4.2</wiremock.version>
		<testcontainers.version>1.19.6</testcontainers.version>
		<bonita-java-client.version>1.0.1</bonita-java-client.version>
		<awaitility.version>4.2.0</awaitility.version>

		<!-- Maven plugins -->
		<maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
		<maven.compiler.release>${java.version}</maven.compiler.release>
		<maven-assembly-plugin.version>3.8.0</maven-assembly-plugin.version>
		<groovy-maven-plugin.version>2.1.1</groovy-maven-plugin.version>
		<groovy.version>3.0.19</groovy.version>
		<maven-surefire-plugin.version>3.2.5</maven-surefire-plugin.version>
		<maven-failsafe-plugin.version>3.2.5</maven-failsafe-plugin.version>
		<jacoco-maven-plugin.version>0.8.11</jacoco-maven-plugin.version>
		<maven-source-plugin.version>3.3.0</maven-source-plugin.version>
		<maven-javadoc-plugin.version>3.7.0</maven-javadoc-plugin.version>
		<central-maven-plugin.version>0.8.0</central-maven-plugin.version>
	</properties>

	<dependencies>
		<!-- Bonita -->
		<dependency>
			<groupId>org.bonitasoft.engine</groupId>
			<artifactId>bonita-common</artifactId>
			<version>${bonita-common.version}</version>
			<scope>provided</scope>
		</dependency>

		<!-- Connector -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<version>${lombok.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
			<version>${jackson.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>${slf4j.version}</version>
			<scope>provided</scope>
		</dependency>

		<!-- Tests -->
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>${junit-jupiter.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-core</artifactId>
			<version>${mockito.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-junit-jupiter</artifactId>
			<version>${mockito.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.assertj</groupId>
			<artifactId>assertj-core</artifactId>
			<version>${assertj.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>net.jqwik</groupId>
			<artifactId>jqwik</artifactId>
			<version>${jqwik.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
			<version>1.4.14</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<defaultGoal>verify</defaultGoal>
		<resources>
			<resource>
				<directory>src/main/resources-filtered</directory>
				<filtering>true</filtering>
			</resource>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>false</filtering>
			</resource>
		</resources>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>${maven-compiler-plugin.version}</version>
					<configuration>
						<annotationProcessorPaths>
							<path>
								<groupId>org.projectlombok</groupId>
								<artifactId>lombok</artifactId>
								<version>${lombok.version}</version>
							</path>
						</annotationProcessorPaths>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-surefire-plugin</artifactId>
					<version>${maven-surefire-plugin.version}</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-failsafe-plugin</artifactId>
					<version>${maven-failsafe-plugin.version}</version>
				</plugin>
				<plugin>
					<artifactId>maven-assembly-plugin</artifactId>
					<version>${maven-assembly-plugin.version}</version>
					<configuration>
						<descriptorSourceDirectory>
							${project.basedir}/src/assembly</descriptorSourceDirectory>
					</configuration>
					<executions>
						<execution>
							<id>package-connectors</id>
							<phase>package</phase>
							<goals>
								<goal>single</goal>
							</goals>
							<configuration>
								<appendAssemblyId>true</appendAssemblyId>
							</configuration>
						</execution>
					</executions>
				</plugin>
				<plugin>
					<groupId>org.codehaus.gmaven</groupId>
					<artifactId>groovy-maven-plugin</artifactId>
					<version>${groovy-maven-plugin.version}</version>
					<dependencies>
						<dependency>
							<groupId>org.codehaus.groovy</groupId>
							<artifactId>groovy</artifactId>
							<version>${groovy.version}</version>
						</dependency>
					</dependencies>
				</plugin>
				<plugin>
					<groupId>org.jacoco</groupId>
					<artifactId>jacoco-maven-plugin</artifactId>
					<version>${jacoco-maven-plugin.version}</version>
					<configuration>
						<append>true</append>
					</configuration>
					<executions>
						<execution>
							<id>prepare-agent</id>
							<goals>
								<goal>prepare-agent</goal>
							</goals>
						</execution>
						<execution>
							<id>prepare-agent-integration</id>
							<goals>
								<goal>prepare-agent-integration</goal>
							</goals>
						</execution>
						<execution>
							<id>jacoco-site</id>
							<phase>verify</phase>
							<goals>
								<goal>report</goal>
							</goals>
						</execution>
					</executions>
				</plugin>
				<plugin>
					<groupId>org.sonatype.central</groupId>
					<artifactId>central-publishing-maven-plugin</artifactId>
					<version>${central-maven-plugin.version}</version>
					<extensions>true</extensions>
					<configuration>
						<autoPublish>true</autoPublish>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>

		<plugins>
			<plugin>
				<groupId>org.codehaus.gmaven</groupId>
				<artifactId>groovy-maven-plugin</artifactId>
				<executions>
					<execution>
						<phase>generate-resources</phase>
						<goals>
							<goal>execute</goal>
						</goals>
						<configuration>
							<source>
								${project.basedir}/src/script/dependencies-as-var.groovy</source>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>${maven-source-plugin.version}</version>
				<executions>
					<execution>
						<id>attach-sources</id>
						<goals>
							<goal>jar-no-fork</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<version>${maven-javadoc-plugin.version}</version>
				<configuration>
					<failOnError>false</failOnError>
					<doclint>none</doclint>
				</configuration>
				<executions>
					<execution>
						<id>attach-javadocs</id>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.sonatype.central</groupId>
				<artifactId>central-publishing-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>deploy</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-gpg-plugin</artifactId>
						<version>3.1.0</version>
						<executions>
							<execution>
								<id>sign-artifacts</id>
								<phase>verify</phase>
								<goals>
									<goal>sign</goal>
								</goals>
								<configuration>
									<gpgArguments>
										<arg>--pinentry-mode</arg>
										<arg>loopback</arg>
									</gpgArguments>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

	<repositories>
		<repository>
			<id>bonitasoft-releases</id>
			<url>https://bonitasoft.jfrog.io/artifactory/maven</url>
		</repository>
	</repositories>

	<scm>
		<developerConnection>scm:git:git@github.com:bonitasoft/bonita-connector-zendesk.git</developerConnection>
		<connection>scm:git:git@github.com:bonitasoft/bonita-connector-zendesk.git</connection>
		<url>https://github.com/bonitasoft/bonita-connector-zendesk</url>
		<tag>HEAD</tag>
	</scm>

</project>
